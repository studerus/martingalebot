# This file is part of the standard testthat package structure.
# It loads the package and runs all tests in the testthat/ subdirectory.

library(testthat)
library(martingalebot)

test_check("martingalebot")